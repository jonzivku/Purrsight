import React from 'react';
import { } from 'react-bootstrap';
import { PictureCard } from '../components/PictureCard';
import { PostingCard } from '../components/PostingCard';

export function PhotoFeed(props) {
//what do we want on a post?
// image, caption
    return (
        <h1>
        { console.log("foo") }
        </h1>
    );

}
